# Superseded, INVALID — and kept on purpose

These experiments predate the instrumental methodology (`gate_harness`). Their
`decision.json` files are marked **INVALID** by the independent verifier — they
carry no `_harness_provenance` — and are **not citable as results**.

They are preserved as material evidence that the methodology found and marked
the project's own errors. The harnessed replays of B1 and B2, produced through
the enforcing pipeline, live in [`../harness_valid/`](../harness_valid/).

Do not restore provenance retroactively: the INVALID status of everything in
this directory is a *result*, not a defect.
